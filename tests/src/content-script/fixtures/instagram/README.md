# Instagram source fixture

`post.html` is a hand-authored, deterministic representation of the standard
`time[datetime]` labels observed on an Instagram publication. It retains only
the timestamp placement, styling hooks, and dynamic-comment container needed
to verify in-place presentation and restoration without network access.
